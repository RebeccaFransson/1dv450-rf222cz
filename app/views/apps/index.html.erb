
<%= render "layouts/loggedIn" %>

<div class="page-header">
  <h1><%=	 current_user.name %>'s applications</h1>
  <%=  link_to "Add application", new_app_path %>
  <%	 if current_user.isAdmin %>
     <br> <%=  link_to "All applications", admins_path %>
  <% end %>
</div>

<table class="table">
  <thead>
  <tr>
    <th>Name</th>
    <th>Key</th>
    <th></th>
  </tr>
  </thead>
  <tbody>
      <%	@apps.each do |app| %>
        <tr>
            <td>
              <strong>
                <%=	 app.app_name %>
              </strong>
            </td>
            <td>
                <%=	 app.key %>
            </td>
            <td>
                <%=  link_to "Remove app", removeApp_path(app), data: { confirm: 'Are you sure?' } %>
            </td>
        </tr>
      <% end %>
  </tbody>
</table>


